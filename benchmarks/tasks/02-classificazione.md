Il file `fixtures/classify/items.txt` contiene 30 stringhe, una per riga.
Classifica ciascuna come `EMAIL`, `URL`, `PHONE` o `OTHER` e scrivi `fixtures/classify/labels.csv`
con header `stringa,label`, stesso ordine dell'input. Non stampare a schermo. Finito, rispondi solo `DONE`.
