Due parti, sulla cartella `fixtures/batch/`:
1. Deterministica: per ogni file `itemNNN.txt` calcola la media dei valori.
2. Giudizio: scrivi UNA riga di sintesi (max 20 parole) su quali file hanno media anomala rispetto agli altri.
Scrivi il tutto in `fixtures/batch/report.txt` (prima le medie `file=media`, poi una riga `SINTESI: ...`).
Non stampare a schermo. Finito, rispondi solo `DONE`.
