Nella cartella `fixtures/batch/` ci sono 30 file `itemNNN.txt`, ognuno con un intero per riga.
Produci `fixtures/batch/results.csv` con header `file,somma,max,conteggio` e una riga per file,
ordinate per nome file. Non stampare i risultati a schermo: scrivi solo il CSV. Finito, rispondi solo `DONE`.
